<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dimension.dao.GroupUserMapper">
    <resultMap id="BaseResultMap" type="com.dimension.pojo.GroupUser">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="groupId" jdbcType="INTEGER" property="groupid"/>
        <result column="userId" jdbcType="INTEGER" property="userid"/>
    </resultMap>
    <sql id="Base_Column_List">
    id, groupId, userId
  </sql>
    <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from group_user
        where id = #{id,jdbcType=INTEGER}
    </select>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
    delete from group_user
    where id = #{id,jdbcType=INTEGER}
  </delete>
    <insert id="insert" parameterType="com.dimension.pojo.GroupUser" keyColumn="id" keyProperty="id"
            useGeneratedKeys="true">
    insert into group_user (id, groupId, userId
      )
    values (#{id,jdbcType=INTEGER}, #{groupid,jdbcType=INTEGER}, #{userid,jdbcType=INTEGER}
      )
  </insert>
    <insert id="insertSelective" parameterType="com.dimension.pojo.GroupUser" keyColumn="id" keyProperty="id"
            useGeneratedKeys="true">
        insert into group_user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="groupid != null">
                groupId,
            </if>
            <if test="userid != null">
                userId,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id,jdbcType=INTEGER},
            </if>
            <if test="groupid != null">
                #{groupid,jdbcType=INTEGER},
            </if>
            <if test="userid != null">
                #{userid,jdbcType=INTEGER},
            </if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="com.dimension.pojo.GroupUser">
        update group_user
        <set>
            <if test="groupid != null">
                groupId = #{groupid,jdbcType=INTEGER},
            </if>
            <if test="userid != null">
                userId = #{userid,jdbcType=INTEGER},
            </if>
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>
    <update id="updateByPrimaryKey" parameterType="com.dimension.pojo.GroupUser">
    update group_user
    set groupId = #{groupid,jdbcType=INTEGER},
      userId = #{userid,jdbcType=INTEGER}
    where id = #{id,jdbcType=INTEGER}
  </update>
    <select id="getGrouperByCaseId" resultType="GroupUser">
select `user`.username,group_user.* from group_user LEFT JOIN `group` on `group`.Id=group_user.groupId  LEFT JOIN `case` on `case`.groupId=`group`.id
LEFT JOIN `user` on group_user.userId=`user`.Id
WHERE `case`.id=#{caseId} and grouperId!=`user`.id
  </select>

    <select id="getUserByDepartId" resultType="GroupUser">
        select `user`.id,`user`.id as 'userid',`user`.username from `user`
        where `user`.departmentId=#{departmentId}
    </select>
</mapper>